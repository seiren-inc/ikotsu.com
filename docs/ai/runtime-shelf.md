# Runtime Shelf — ikotsu.com

Always-on: 7 skills in `.claude/skills/`.
See `.claude/skills-runtime/README.md` for grouped catalog.

## Runtime-Only Categories

| Category | Reason |
|----------|--------|
| DB / Prisma sprints | Infrequent; `db-safe-update` from workspace vault |
| Animation sprints | Periodic |
| Performance sweeps | Sprint-only |
| Design import | Infrequent |

Context budget: core 7 + max 3 runtime = ≤10 active.
